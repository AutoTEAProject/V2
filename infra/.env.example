DB_NAME=autotea
DB_USERNAME=autotea
DB_PASSWORD=autotea
CALC_TIMEOUT_SECONDS=300

# openssl rand -base64 48 등으로 생성한 무작위 문자열(32바이트 이상) 사용
JWT_SECRET=
# Google Cloud Console > OAuth 클라이언트 ID (웹 애플리케이션)
GOOGLE_CLIENT_ID=
CORS_ALLOWED_ORIGINS=http://localhost:5173
